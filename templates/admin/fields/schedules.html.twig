{% set url = ea_url()
    .set('filters[merchant][comparison]',"=")
    .set('filters[merchant][value]', field.value.owner.id)
    .setController('App\\Controller\\Admin\\ScheduleCrudController')
    .setAction('index')
%}

{% if field.value|length > 0 %}
    <a href="{{ url }}">--{{ field.value|length }}--</a>
{% endif %}
