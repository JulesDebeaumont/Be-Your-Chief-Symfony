{% extends 'base.html.twig' %}

{% block body %}
<div class="Connexion">

     <div class="LoginFormInputsRegister">
            <div>
                <small class="CheckPassword">
                    Un mail contenant le lien pour réinitialiser votre mot de passe à était envoyer.<br/>
                    Ce lien expirera dans {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.
                </small>
            </div>
        </div>
        <div class="allButtons">
            <a href="{{ path('app_login') }}" class="btn btn-lg btn-primary loginButton" type="button" style="margin-top: 30px;  background-color: rgb(13, 13, 13);">
                Retour à la connexion
            </a>
        </div>

    </div>
{% endblock %}
